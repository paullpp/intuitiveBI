services:
  backend:
    build:
      context: ./src/backend
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./src/frontend
    ports:
      - "8000:80"
    depends_on:
      - backend
